{ ... }:

{
  # ===========================================================================
  # Security Configuration
  # ===========================================================================

  # --- Firewall ---
  # 外部からの不正アクセスをブロック
  # アウトバウンド通信（ブラウジング、API通信等）には影響なし
  networking.firewall.enable = true;
}
